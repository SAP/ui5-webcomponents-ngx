{
  "$schema": "http://json-schema.org/schema",
  "$id": "add-theming-schematic",
  "title": "Adds theming",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
