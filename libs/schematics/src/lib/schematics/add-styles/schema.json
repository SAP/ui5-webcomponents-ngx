{
  "$schema": "http://json-schema.org/schema",
  "$id": "add-styles-schematic",
  "title": "Adds styles",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  }
}
