{
  "$schema": "http://json-schema.org/schema",
  "$id": "sync-schematic",
  "title": "Synchronizes components with the project's configuration.",
  "type": "object",
  "properties": {
    "conf": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "An array of paths to the configuration files.",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "basePath": {
      "type": "string",
      "description": "The base path to the output directory."
    }
  },
  "required": [
    "conf"
  ]
}
